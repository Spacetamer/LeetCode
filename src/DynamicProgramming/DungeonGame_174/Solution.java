package DynamicProgramming.DungeonGame_174;

class Solution {
    public int calculateMinimumHP(int[][] dungeon) {
        for (int i=1; i<dungeon.length; ++i){
            for (int j=1; j<dungeon[0].length; ++j){
                //dungeon[i][j] = Math.max(dungeon[])
            }
        }

        return 0;
    }
}
